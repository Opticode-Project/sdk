// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

export class ArrayType {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
__init(i:number, bb:flatbuffers.ByteBuffer):ArrayType {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsArrayType(bb:flatbuffers.ByteBuffer, obj?:ArrayType):ArrayType {
  return (obj || new ArrayType()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsArrayType(bb:flatbuffers.ByteBuffer, obj?:ArrayType):ArrayType {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ArrayType()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

elem():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

size():flatbuffers.Long {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : this.bb!.createLong(0, 0);
}

static startArrayType(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addElem(builder:flatbuffers.Builder, elem:number) {
  builder.addFieldInt32(0, elem, 0);
}

static addSize(builder:flatbuffers.Builder, size:flatbuffers.Long) {
  builder.addFieldInt64(1, size, builder.createLong(0, 0));
}

static endArrayType(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createArrayType(builder:flatbuffers.Builder, elem:number, size:flatbuffers.Long):flatbuffers.Offset {
  ArrayType.startArrayType(builder);
  ArrayType.addElem(builder, elem);
  ArrayType.addSize(builder, size);
  return ArrayType.endArrayType(builder);
}
}
